# InternalApiFilter 가 요구한다. 이 서비스는 config-server 를 쓰지 않으므로 여기서 직접 정의한다.
modu:
  internal-api:
    token: ${INTERNAL_API_TOKEN:modu-internal-dev-token}

server:
  port: 8080

spring:
  profiles:
    active: dev
