name: modu-messenger

# 인프라(MySQL, MongoDB, Redis, Kafka, RabbitMQ, MinIO)와 관측성 스택은 ~/workspace/modu_infra 에서,
# 플랫폼(config-service, discovery-service, gateway-service)은 ~/workspace/modu_platform 에서 먼저 띄운다.
# 서비스는 external 네트워크 modu-infra 에서 컨테이너 이름으로 인프라·플랫폼에 접근한다.
# 다른 compose 프로젝트라 depends_on 을 걸 수 없다. 설정을 못 받으면 서비스가 기동에 실패하고(fail-fast)
# restart: always 가 다시 띄우므로, 플랫폼이 뜨면 자연히 따라 올라온다.

services:

  auth-service:
    build:
      context: ./auth-service
      dockerfile: ./Dockerfile
    container_name: auth-service
    ports:
      - 9900:9900
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - JAVA_TOOL_OPTIONS=${AUTH_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
      - redis-cluster
    restart: always

  chat-service:
    build:
      context: ./chat-service
      dockerfile: ./Dockerfile
    container_name: chat-service
    ports:
      - 9090:9090
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql-chat:3306/modu-chat
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${CHAT_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

  member-service:
    build:
      context: ./member-service
      dockerfile: ./Dockerfile
    container_name: member-service
    ports:
      - 8080:8080
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql-member:3306/modu-chat
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${MEMBER_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
      - redis-cluster
    restart: always

  profile-service:
    build:
      context: ./profile-service
      dockerfile: ./Dockerfile
    container_name: profile-service
    ports:
      - 8880:8880
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql-profile:3306/modu-chat
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${PROFILE_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

  push-service:
    build:
      context: ./push-service
      dockerfile: ./Dockerfile
    container_name: push-service
    ports:
      - 9990:9990
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql-push:3306/modu-chat
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${PUSH_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

  storage-service:
    build:
      context: ./storage-service
      dockerfile: ./Dockerfile
    container_name: storage-service
    ports:
      - 9999:9999
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql:3306/modu-chat
      - spring.datasource.username=root
      - minio.endpoint=http://minio:9000
      - JAVA_TOOL_OPTIONS=${STORAGE_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    networks:
      - modu-infra
      - pinpoint
    restart: always
    volumes:
      - /var/modu-chat/storage/data:/data
      - pinpoint-agent:/pinpoint-agent:ro

  ws-service:
    build:
      context: ./ws-service
      dockerfile: ./Dockerfile
    container_name: ws-service
    ports:
      - 8090:8090
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - spring.datasource.url=jdbc:mysql://mysql:3306/modu-chat
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${WS_SERVICE_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

  point-service:
    build:
      context: ./point-service
      dockerfile: ./Dockerfile
    container_name: point-service
    ports:
      - 9600:9600
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      # mysql-member 인스턴스의 별도 스키마(modu-point). 스키마는 point-service/sql/schema.sql 로 한 번 만든다.
      - spring.datasource.url=jdbc:mysql://mysql-member:3306/modu-point
      - spring.datasource.username=root
      - JAVA_TOOL_OPTIONS=${POINT_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

  chat-store-service:
    build:
      context: ./chat-store-service
      dockerfile: ./Dockerfile
    container_name: chat-store-service
    ports:
      - 8100:8100
    environment:
      - CONFIG_SERVER_URI=http://config-service:8888
      - JAVA_TOOL_OPTIONS=${CHAT_STORE_AGENT_OPTS:-}
      - INTERNAL_API_TOKEN=${INTERNAL_API_TOKEN}
    volumes:
      - pinpoint-agent:/pinpoint-agent:ro
    networks:
      - modu-infra
      - pinpoint
    restart: always

networks:
  modu-infra:
    external: true
  pinpoint:
    external: true
    name: pinpoint-docker_pinpoint
  # Redis 클러스터 노드는 modu_infra 의 클러스터 전용 네트워크 IP(10.90.0.x)를 광고한다.
  # Redisson(member-service)은 그 IP 로 붙으므로 auth-service·member-service 는 이 네트워크에도 참여한다.
  redis-cluster:
    external: true
    name: modu-data_redis-cluster

volumes:
  pinpoint-agent:
    external: true
    name: pinpoint-docker_data-volume
