spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: message
      profile: native

management:
  endpoints:
    web:
      exposure:
        include: health, beans, refresh, busrefresh